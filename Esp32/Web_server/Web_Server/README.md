This is a simple tutorial how can you make a esp32 webserver of your own

This can control one led from web (led is connected to the GPIO23 of esp )

When the esp initializes then an ip is found on the serial monitor to access the server.